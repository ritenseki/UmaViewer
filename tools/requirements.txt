# Python dependencies for the UmaViewer asset tools (tools/*.py).
#
#   python3 -m venv ~/.venvs/umatools
#   ~/.venvs/umatools/bin/python -m pip install -r tools/requirements.txt
#
# NOTE: it must be `apsw-sqlite3mc`, NOT plain `apsw`.  Only that wheel bundles
# SQLite3 Multiple Ciphers, which is what decrypts the game's `meta` database
# (the same library the C# side loads as sqlite3mc_x64.dll).

UnityPy>=1.20
apsw-sqlite3mc>=3.46
